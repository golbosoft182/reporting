export class OrganicChannel{
    private key:string;
    private color:string;
    public values;
    constructor(key,color,values){
        this.key=key;
        this.color=color;
        this.values=values;
    }
}